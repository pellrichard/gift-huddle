export const dynamic = "force-dynamic";
export const revalidate = 0;

export default async function AccountPage() {
  // Placeholder page shell; real data fetching can be added once helpers are finalized.
  return null;
}
