// src/lib/supabase/server-rsc.ts
// Stub that re-exports from pages-safe helper to avoid `next/*` imports at build time.
export { createServerSupabase, createRouteHandlerSupabase, supabaseServer } from "./server";
